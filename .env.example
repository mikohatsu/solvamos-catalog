PORT=4173
PUBLIC_BASE_URL=http://localhost:4173
STUDIO_URL=https://solvamos-studio-74094114833.asia-northeast3.run.app

# Shared secret for Studio → catalog writes (required in production)
CATALOG_ADMIN_SECRET=dev-catalog-admin-secret-change-me

# Persistent JSON store (Cloud Run defaults to /tmp/...)
# CATALOG_STORE_PATH=.data/catalog-store.json
